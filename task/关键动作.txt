检查appeal_ticket、ticket_event、rule_catalog和sla_policy的主键、父链、UTC时间及JSON结构。
读取report_contract.json和queue_assignments.csv，取得截止时间、事件动作、报告清单与队列负责人。
使用递归CTE沿parent_ticket_id追溯根工单，在访问路径中识别缺失父级和循环引用。
建立有效工单到根线程的映射，把父链异常留在quality_exceptions。
用json_each展开rule_hits并关联rule_catalog，按线程和规则归集命中与自动升级。
汇总线程成员、开放状态、严重级别、地区、队列、责任团队和事件时间边界。
用unixepoch计算首次响应与最后终态相对线程起点的分钟差，再关联sla_policy判断时效。
从派生表导出四份CSV和audit_summary.json，核对根线程集合、责任归属和控制量。
运行npm run audit重建交付物，确认四张源表保持原样，数据库与报告口径一致。
